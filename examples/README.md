# Praxis Examples

This directory contains examples demonstrating how to use the Praxis game engine.

## Available Examples

### Simple Praxis

A minimal example that initializes and runs the Praxis engine with default settings.

To run:

```bash
cargo run --package hello_praxis
```
