[package]
name = "praxis_graphics"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"

[dependencies]
wgpu = { version = "25.0.0", default-features = false, features = ["vulkan", "metal"] }
pollster = "0.4.0"
raw-window-handle = "0.6.2"
winit = "0.30.10"
praxis_utils = { path = "../praxis_utils", version = "0.1.0" }
