[package]
name = "praxis_graphics"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"

[dependencies]
vulkano = "0.34.0"
pollster = "0.4.0"
raw-window-handle = "0.6.2"
winit = { version = "0.30.10", features = ["rwh_05"] }
praxis_utils = { path = "../praxis_utils", version = "0.1.0" }
