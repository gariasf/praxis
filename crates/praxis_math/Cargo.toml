[package]
name = "praxis_math"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
# SIMD-optimized 3D math library with serialization support
glam = { version = "0.30.4", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }

# Internal dependencies
praxis_utils = { path = "../praxis_utils" }
